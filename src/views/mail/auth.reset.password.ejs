<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= __('Reset Password') %></title>
  </head>
  <body>
    <p><%= __('Your OTP is:') %> <strong><%= otp %></strong></p>
    <p><%= __('This OTP will expire in:') %> <strong><%= expirationTime %> minutes</strong></p>
    <p><%= __('If you did not request a password reset, please ignore this email.') %></p>
    <p><%= __('Thank you for your attention!') %></p>
  </body>
</html>
