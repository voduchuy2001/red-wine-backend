class UserDto {
  constructor(user) {
    this.avatar = user.avatar
    this.name = user.name
    this.email = user.email
    this.lastLoginAt = user.password
    this.dob = user.lastLoginAt
  }
}

export default UserDto
